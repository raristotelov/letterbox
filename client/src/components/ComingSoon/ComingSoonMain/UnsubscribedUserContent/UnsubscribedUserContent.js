import SubscribeForm from '../../ComingSoonSubscribeForm';
import './UnsubscribedUserContent.scss';

const UnsubscribedUserContent = () => {
    return (
        <section className="unsubscribed-section">
            <h1>Bring back the joy of reading newsletters</h1>

            <SubscribeForm />
        </section>
    )
}

export default UnsubscribedUserContent;